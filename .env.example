# Host port for the frontend (nginx)
FRONTEND_PORT=80

# Host port for the backend (Go API)
BACKEND_PORT=8081

# API base URL baked into the frontend SPA at build time
# /api  → nginx reverse proxy (default, production-safe)
# http://localhost:8081 → direct backend access (useful for dev)
VUE_APP_API_URL=/api
